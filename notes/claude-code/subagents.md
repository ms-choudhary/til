# Subagents

Use subagents when side tasks would flood main conversation. Works in it's own context and returns only the summary. 

### Why

- Preserve context
- Control costs by routing to cheaper models

### Advisor tool

Set `/advisor`, which lower models can consult for architectural and other complex decisions. 