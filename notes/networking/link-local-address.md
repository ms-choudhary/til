# Link Local Address

Address in IPv4 169.254.0.0/16 block are used for Link Local Adderss. These addresses are automatically assigned by the host, using known standard algorithm, used when no DHCP is found. Not forwarded by the router. 

Used for networking configuration in devices (like Printer) on the network without DHCP. 